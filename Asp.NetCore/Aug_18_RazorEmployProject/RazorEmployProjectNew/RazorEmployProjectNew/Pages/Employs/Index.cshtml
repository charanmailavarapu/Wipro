@page
@model RazorEmployProjectNew.Pages.Employs.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(Model => Model.EmployNew[0].Empno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployNew[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployNew[0].Gender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployNew[0].Dept)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployNew[0].Desig)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployNew[0].Basic)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EmployNew) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Empno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dept)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Desig)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Basic)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Empno">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Empno">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Empno">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
